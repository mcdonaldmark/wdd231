const gridbutton = document.querySelector("#grid"), listbutton = document.querySelector("#list"), display = document.querySelector("article"); function showList() { display.classList.add("list"), display.classList.remove("grid") } gridbutton.addEventListener("click", () => { display.classList.add("grid"), display.classList.remove("list") }), listbutton.addEventListener("click", showList);